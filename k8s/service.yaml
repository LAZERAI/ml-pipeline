# Kubernetes Service for ML Pipeline
# ===================================

apiVersion: v1
kind: Service
metadata:
  name: ml-pipeline-service
  labels:
    app: ml-pipeline
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ml-pipeline
